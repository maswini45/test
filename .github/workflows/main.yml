name: Maven CD

on: [push]
 
jobs:
 deploy:
  runs-on: windows-latest
  steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: Run build script
      run: |
          echo "success"
          ls
          pwd
          echo "testing for documentation"
          echo "revert testing"